//导出文件名 必须
out_name = cfg_resource_retrieve

//key值 必须
key = id

//value字段值 必须
value = c_resource_retrieve;id;名称,string;次数计算,list;基础上限次数;额外上限天数;等级经验倍率;实际奖励,string;副本ID;单次银两;单次绑元;额外次数单次绑元,list

is_list = true
