//导出文件名 必须
out_name = cfg_mission_excel

//key值 必须
key = 任务id

//value字段值 必须
value = c_mission_excel;任务id;任务名称,string;章节id;类型;组ID;品质;环数;轮数;前置任务ID;后置任务ID;任务最低等级;任务最高等级;是否需要帮派;转生限制,list;自动接取任务;自动完成任务;任务类型;目标参数,string;经验奖励类型;经验奖励;道具奖励,string;加速buff,list;关联系统ID

is_list = true